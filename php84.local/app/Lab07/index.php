<?php
/*
 * Task 4 (20%). Use `index.php` to:
 *    - Start the session.
 * 	  - Attempt to get the username and favourite colour stored in the session and cookie respectively and store in local vars.
 * 	  - Create a simple HTML page titled Home. On this page:
 *    	- Display links to log in (if no user is logged in) or log out (if a user is logged in).
 *    	- Display a personalized welcome message using the user's name and the user's favorite color (if logged in).
 */
